// Beta-testing switch: pricing/subscriptions are off, rehearsals are
// unlimited, and every audition requires a piece of feedback before the
// user can keep using the app (see FeedbackGate.tsx). Flip to false — and
// restore the /pricing and /billing routes in App.tsx — once beta ends.
export const IS_BETA_TESTING = true;
